# Frontend API base URL (must include /api suffix)
VITE_API_URL=http://localhost:5000/api

# Public site URL used in QR codes (must match where the frontend is served)
# Backend CLIENT_URL should match this value
